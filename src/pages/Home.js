import React from "react";
import AppLayout from "../components/AppLayout";
import { Toolbar } from "@mui/material";

const Home = () => {
  return (
    <AppLayout>
      <Toolbar />
    </AppLayout>
  );
};

export default Home;
